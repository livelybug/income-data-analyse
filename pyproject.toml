[project]
name = "income-data-analyse"
version = "0.1.0"
description = "UCI Adult (Census Income) 数据集完整 EDA + 机器学习建模"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
    "seaborn>=0.13",
    "scikit-learn>=1.3",
    "jupyter>=1.0",
    "nbconvert>=7.0",
    "ipykernel>=6.0",
]

[tool.uv]
package = false
